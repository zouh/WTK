<li>
  <%= gravatar_for user, size: 52 %>
  <%= link_to user.name, user %>
  <% if (current_user.admin? || current_member.master?) && !current_user?(user) %>
    | <%= link_to "删除", user, method: :delete,
                                  data: { confirm: "你确定吗?" } %>
  <% end %>
</li>