<% provide(:title, '新增商品') %>
<h1>新增商品</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@product) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "新建商品", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
